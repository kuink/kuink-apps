<?xml version='1.0'?>
<Method>
    <Doc>
        Get the companies of a given person
    </Doc>
    <Params>
        <Param name="id_person"/>
    </Params>
    <Body>
        <Sql>
            <![CDATA[        
                SELECT
                    c.id,
                    c.code,
                    c.name,
                    uc.is_default,
                    uc.is_active
                FROM 
                    fw_user_company uc
                    INNER JOIN fw_company c ON (c.id = uc.id_company)
                WHERE  
                  uc.id_person = {param->id_person}
                  AND c.is_active = 1
                  AND uc.is_active = 1
                ORDER BY  
                  c.code
        ]]>
        </Sql>
    </Body>
</Method>
